str mots[2];
mots[0] = "Bonjour";
mots[1] = "Monde";

print("Test .length sur tableau:");
print(mots[0].length);

print("Test .substring sur tableau:");
str extrait = mots[0].substring(0, 3);
print(extrait);
